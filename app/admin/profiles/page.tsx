import { UserMinus } from "lucide-react"; import { asc } from "drizzle-orm"; import { getDb } from "../../../db"; import { cmsProfiles } from "../../../db/schema"; import { AdminLayout } from "../AdminLayout"; import { requireCmsAdmin } from "../auth"; import { createProfile, toggleProfile } from "../taxonomy-actions";
export const dynamic="force-dynamic";
export default async function ProfilesPage(){const user=await requireCmsAdmin(),db=await getDb(),all=await db.select().from(cmsProfiles).orderBy(asc(cmsProfiles.name));return <AdminLayout userName={user.displayName} active="/admin/profiles"><header className="cms-top"><div><p>Equipa</p><h1>Perfis</h1></div></header><div className="cms-table"><form action={createProfile} className="cms-inline-form"><input name="name" placeholder="Nome" required/><input name="email" type="email" placeholder="email@empresa.com" required/><select name="role"><option value="editor">Editor</option><option value="admin">Administrador</option></select><button className="cms-primary">Adicionar perfil</button></form>{all.map(profile=><div className="cms-table__row" key={profile.id}><strong>{profile.name}</strong><span>{profile.email} · {profile.role}</span><form action={toggleProfile.bind(null,profile.id,profile.status==="active"?"inactive":"active")}><button className="cms-icon-button" aria-label="Alterar estado"><UserMinus size={16}/></button></form></div>)}</div></AdminLayout>}
